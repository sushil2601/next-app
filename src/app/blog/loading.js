
const Loading = () =>{
    return <h1>Loading...</h1>

}

export default Loading;

//behind the scene loading component is automatically wrap the page.js and it's nested child within a react suspense boundary.